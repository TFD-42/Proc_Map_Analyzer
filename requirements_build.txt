# Build-only dependency (build.sh / build.ps1): PyInstaller, pinned so two
# builds of the same commit produce the same bundle layout. Runtime
# dependencies stay in requirements_frozen.txt. Verified against the PyPI
# index on 2026-09-07 (pip index versions pyinstaller -> 6.22.2 latest).
pyinstaller==6.22.2
