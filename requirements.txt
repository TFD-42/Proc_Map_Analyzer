# Dépendances de analyseur_processus_allinone.py (et de l'ancien
# process_graph_analyzer.py).
#
# psutil n'est PAS installable sur Android/Termux (le script bascule alors
# automatiquement sur un backend /proc fait maison, voir H14 dans l'en-tête
# de analyseur_processus_allinone.py) — sur Termux, installe uniquement les
# trois autres lignes, ou utilise install.sh qui gère déjà cette exception
# automatiquement.
#
# Bornes basses = versions réellement testées en développement (macOS,
# Python 3.9) et exercées par la CI (3.9/3.11/3.12). Pas de bornes hautes :
# aucune incompatibilité connue à ce jour — à resserrer si la CI en révèle.
networkx>=3.2.1
matplotlib>=3.11.1
requests>=2.32.5
psutil>=7.2.2
