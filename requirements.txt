# Dependencies of process_analyzer_allinone.py (and of the older
# process_graph_analyzer.py).
#
# psutil is NOT installable on Android/Termux (the script then automatically
# falls back to its own custom /proc backend, see H14 in the header of
# process_analyzer_allinone.py) — on Termux, install only the three
# other lines, or use install.sh, which already handles this exception
# automatically.
#
# Lower bounds = versions actually tested in development (macOS,
# Python 3.9) and exercised by CI (3.9/3.11/3.12). No upper bounds:
# no known incompatibility so far — tighten if CI reveals one.
networkx>=3.2.1
matplotlib>=3.9.4
requests>=2.32.5
psutil>=7.2.2
